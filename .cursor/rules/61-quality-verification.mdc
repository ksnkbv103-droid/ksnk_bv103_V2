---
description: "[Superseded] Verify checklist — dùng 03-src-editing-compact; @mention trước merge lớn"
alwaysApply: false
---

# Chất lượng & bàn giao

> **Superseded** cho slice thường: [`03-src-editing-compact.mdc`](./03-src-editing-compact.mdc).  
> Invoke `@61-quality-verification` trước merge PR lớn.

- CRUD / lọc / import-export / dashboard nếu đụng.
- Mapping FK hiển thị; RLS khi dữ liệu có mà UI trống.
- `permission-registry` + mapping; migration an toàn; test tay UI luồng vừa sửa.
