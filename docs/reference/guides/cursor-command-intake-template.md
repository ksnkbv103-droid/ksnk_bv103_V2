# Cursor Command Template: Intake

**Preferred:** type `/intake` in Cursor (see `.cursor/commands/intake.md`).

Use this prompt at the start of a task if slash commands are unavailable.

```text
[INTAKE]
Goal:
In scope files/modules:
Out of scope:
Acceptance criteria:
Constraints:
Verify commands:

Do not code yet.
Return:
1) 3-5 step plan
2) risks (top 3)
3) assumptions needing confirmation
```

