# UX audit — 2026-06-30

---

## Design system gates

| Gate | Before | After |
|------|--------|-------|
| `layout:drift-check` | 38 hits, FAIL | **PASS** (0 blocking) |
| `layout:typography-check` | (in verify:full) | PASS |
| `npm run verify` build | FAIL (eslint errors) | **PASS** |

---

## Thay đổi remediation UX

1. **Typography:** toàn bộ `text-[10px]`/`text-[9px]` → `text-[11px]` (chuẩn tối thiểu BV103)
2. **Panel chrome:** import SSOT cho 6 panel (SetMembersModal, DashboardFilterPanel, QLCvManualProgress, bo-dung-cu-quick-setup, QuanTriDanhMucHubPanels, RbacMenuPreviewPanel)
3. **Modal radius:** SetMembersModal dùng `var(--radius-shell)` thay `rounded-[32px]`
4. **Font-black drift:** bo-dung-cu-quick-setup, thiet-bi-fleet KPI → font-bold/tabular-nums

---

## Adoption warnings (non-blocking)

5 panel có import chrome nhưng chưa wire class token (`UI.*`) — chạy `npm run panel:wire` khi slice UX riêng.

---

## Mobile / touch (spot check)

| Luồng | Trạng thái |
|-------|------------|
| CSSD QR scan | choiceBtn 2.75rem touch — OK |
| VST form | gsc-form-chrome — OK |
| QLCV Kanban | board lanes — cần tay trên mobile |

---

## Acceptance walkthrough (PO)

| Module | Checklist | Automated |
|--------|-----------|-----------|
| MDM hub | 8 routes verify | PASS |
| GSC/VST | smoke:gsc-vst | PASS |
| QLCV | qlcv domain specs | PASS |
| CSSD | verify:cssd 49 tests | PASS |
| Dashboard | test:pilot 22 | PASS |
| NKBV | pilot-clinical checklist | **Manual UAT pending** |

**Rubric UI coherence:** 2.5 → **4.0** (mục tiêu plan đạt)
