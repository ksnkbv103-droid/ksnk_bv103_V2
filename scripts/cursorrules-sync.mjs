#!/usr/bin/env node
/**
 * Trước đây: đồng bộ PatrickJS/awesome-cursorrules → `.cursor/rules/awesome/`.
 * BV103 lean: bộ `awesome` đã gỡ — dùng `AGENTS.md`, `LEAN_EXECUTION_BV103.md`, ESLint/tsconfig.
 */
console.warn(
  "[cursorrules:sync] Không chạy đồng bộ ngoài. Nếu cần rule tham khảo: clone awesome-cursorrules riêng, không ghi vào repo BV103.",
);
process.exit(0);
