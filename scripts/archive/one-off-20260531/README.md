# One-off scripts (archived 2026-05-31)

Các script cutover / import CSV / parse markdown dùng **một lần** khi migrate dữ liệu pilot.
Không nằm trong `package.json` — không chạy trong CI.

Giữ lại trong git để tham chiếu lịch sử cutover; app runtime không import.
