/** Heuristic máy hấp hơi nước — SSOT: `cssd-sterilizer-kind` (mã `LM_*`). */
export {
  classifySterilizerKind,
  isSteamSterilizerKind as isSteamSterilizerProfile,
  isWasherLoaiMay,
} from "@/lib/domain/cssd-sterilizer-kind";
